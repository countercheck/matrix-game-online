# Nixpacks configuration for server
# https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

# Skip the auto-detected install phase - buildCommand handles it
[phases.install]
cmds = []

[variables]
NODE_VERSION = "20"
